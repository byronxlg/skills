# Project notes

Before substantive work in a project repo, read `~/repos/obsidian/Projects/<cwd basename>.md` if it exists and use it (decisions, conventions, open TODOs) as context. Exact path only; if it is missing, carry on silently and do not create one unasked. Skip when the cwd is itself a vault.
